{
  "slug": "arrays",
  "title": "Arrays",
  "sections": [
    {
      "id": 1,
      "label": "Easy",
      "level": "easy",
      "problems": [
        {
          "id": "arrays-easy-01",
          "title": "Running Sum",
          "prompt": "Given an integer array nums, return a running sum array where runningSum[i] = sum(nums[0]…nums[i]).",
          "examples": [{ "input": "nums = [1,2,3,4]", "output": "[1,3,6,10]" }],
          "constraints": ["1 <= nums.length <= 1000"],
          "starter": {
            "java": "class Solution {\n    public int[] runningSum(int[] nums) {\n        // write your code\n        return nums;\n    }\n}\n",
            "python": "class Solution:\n    def runningSum(self, nums: list[int]) -> list[int]:\n        # write your code\n        return nums\n"
          },
          "solution": {
            "java": "class Solution {\n    public int[] runningSum(int[] nums) {\n        for (int i = 1; i < nums.length; i++) nums[i] += nums[i - 1];\n        return nums;\n    }\n}\n",
            "python": "class Solution:\n    def runningSum(self, nums: list[int]) -> list[int]:\n        for i in range(1, len(nums)):\n            nums[i] += nums[i - 1]\n        return nums\n"
          },
          "samples": [{ "input": "[1,2,3,4]", "output": "[1,3,6,10]" }]
        }
      ]
    },
    { "id": 2, "label": "Intermediate", "level": "intermediate", "problems": [] },
    { "id": 3, "label": "Hard", "level": "hard", "problems": [] }
  ]
}
